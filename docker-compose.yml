db:
  image: mongo
  volumes:
    - data/runtime/db:/data/db
    - data/dump:/dump
  command: mongod --smallfiles
web:
  image: rocket.chat
  env_file:
    - ./.env
  environment:
    - MONGO_URL=mongodb://db:27017/meteor
  links:
    - db:db
  volumes:
    - logs:/home/app/logs
